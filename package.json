{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "verify:deployment-topology": "node scripts/verify-deployment-topology.mjs",
    "verify:post-publish": "node scripts/verify-post-publish-attestation.mjs",
    "test:post-publish": "node --test scripts/post-publish-attestation-core.test.mjs",
    "build:deploy": "pnpm run verify:deployment-topology && pnpm --filter @workspace/futures-web run build && pnpm --filter @workspace/api-server run build",
    "start:deploy": "NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
